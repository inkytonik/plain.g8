/**
 * Main program for $organization$.$name$.
 */
object Main {

    import Factorial.factorial

    def main (args : Array[String]) {
        println ("Hello from $organization$.$name$ Main!")
        println ("Factorial 4 = " + factorial (4))
    }

}
